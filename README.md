# Skills and Agents

OpenCode agent definitions and skill files.

## Structure

- `agents/` — Self-contained agent definitions with YAML frontmatter
- `skills/` — Skill files for various workflows (code review, bootstrapping, etc.)
